#include <stdio.h>
#include "main.h"
/**
 * print_buffer - prints a buffer.
 * @b: the buffer
 * @size: size of b
 * Return: void
 */
void print_buffer(char *b, int size)
{
	printf("buffer %s, size: %d", b, size);
}
