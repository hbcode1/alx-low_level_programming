#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number.
 * @n: number
 * Return: 
 * if natural square root.
 * else -1
 */
int _sqrt_recursion(int n)
{
	if (n == 0)
		return (1);
	if (n < 0)
		return (0);
	return (
}
